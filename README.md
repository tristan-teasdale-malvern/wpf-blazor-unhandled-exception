When an enhandled excpetion is thrown from a Razor coponent in a WPF Blazor app, it causes the window to freeze and become completely unresponsive.

To recreate issue:
1. Build the Solution using Visual Studio
2. Run the app
3. Click the "Click me" to trigger the unhandled exception.
